// Binary Search Trees
// balancing algorithm if "i dont find that often, but i insert a lot" - use Red-Black algorythm
// if "i find often but insert rarely", AVL balancing algorythm is prefered

// Depth First Search
// We go to the very depth, and then unwind to find another one, and another one... until we get the needle!
//
// Breadth First Search
// We go to children of current head, then we through the children childrens (grandsons) ... and so on, until we get the needle!

export default function dfs(head: BinaryNode<number>, needle: number): boolean {
    
}
